# apps/unreal-engine/plugins

Unreal Engine plugins (.upack files). Examples: Cesium for Unreal, custom UE plugins. Do NOT upload the UE engine itself - install via Epic Games Launcher on an EC2 workstation, then snapshot as a custom AMI (ilyrium-ue57-base).

